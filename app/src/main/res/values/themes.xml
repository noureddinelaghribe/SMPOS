<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.StockManagment" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorOnPrimaryContainer">@color/white</item>
        <item name="colorPrimaryContainer">@color/red</item>
    </style>

    <style name="Theme.StockManagment" parent="Base.Theme.StockManagment" />

    <style name="CustomButtonStyle" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="backgroundTint">@android:color/white</item>
        <item name="elevation">0dp</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:padding">0dp</item>
    </style>

</resources>